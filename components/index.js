// index.js inside of COMPONENTS DIRECTORY
// Grab everything from the local header file that is being exported and re-export it out as 'Header';

// The string must match the name of the file, but does not include the file extension
// The variable name can be anything, but should reflect what is represents

export {default as Header} from './Header';
export {default as Navigation} from './Navigation';
export {default as Main} from './Main';
export {default as Footer} from './Footer';
